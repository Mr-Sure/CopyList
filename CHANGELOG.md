# 更新日志

本文件记录 CopyList 所有对用户可感知的变更。

格式参照 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

> **维护约定（人和 AI 都必须遵守）**
>
> 1. 本文件是**发布文案的唯一数据源**：`Scripts/build.sh` 会读取本次版本的条目，
>    同时用于 **commit message 正文** 与 **GitHub Release notes**，因此这里写什么，用户就看到什么。
> 2. 每条只写「用户能感知的变化」，不写文件名、函数名等实现细节。
> 3. 分类固定为：`### 新增`、`### 修复`、`### 优化`、`### 变更`、`### 文档`；无内容的分类直接省略。
> 4. 版本标题必须为 `## [x.y.z] - YYYY-MM-DD`，分类标题必须为三级标题 `###`，脚本按此解析。
> 5. 发布前把新版本条目补在**最上方**；若缺失，`build.sh` 会自动插入一条草稿并提示补充。

## [未发布]

## [1.3.25] - 2026-09-16

### 优化

- **列表图片项与文本、文件项统一视觉风格**：此前图片用 48pt 实心缩略图、其它类型用 36pt 符号槽，导致图片行的预览文本左起点比其它行右移、可用文本宽度也更窄；现三类统一为 40pt 圆角图标槽（文本/文件为低饱和类型底色 + 类型符号，图片由缩略图铺满），图片的**加载中与加载失败态也与其它类型完全同构**，不再出现独立的灰色方块
- 图片缩略图对极端长宽比（长截图、超宽图，超过 3:1）自动改用完整显示，避免按正方形裁剪后只剩中间一条
- 主窗口列表同步统一：图标槽与状态栏弹窗风格一致，并为图片项补上缩略图（此前只显示类型符号）
- 图片图标槽补充无障碍标签；缩略图加载逻辑抽为弹窗与主窗口共用的实现，避免两处策略漂移

## [1.3.24] - 2026-09-14

### 修复

- **全屏下状态栏图标偶发不显示（或找不到）**：应用内首次修复 —— 图标资源改为多路径查找（App 包资源接口 → `resourcePath` → 源码目录），全部失败时回退系统符号并打印日志，不再静默留空；状态项按钮尚未就绪时自动重试，覆盖登录项冷启动时序
- **构建产物缺失状态栏图标**：`Scripts/build.sh` 此前从未拷贝 `statusbar_icon.png`，全新 clone 构建出的 App/DMG 完全没有状态栏图标，现已在打包阶段补齐（并在缺少 `AppIcon.icns` 时由 `AppIcon.iconset` 自动生成）
- **列表序号溢出**：序号列宽度固定 24pt，历史满 1000 条、收藏数量不设上限时，3 位以上序号被截断并压到类型图标上；现按数字位数动态加宽，并加等宽数字与缩放下限兜底
- **标签过多或过长时撑破整行**：标签胶囊的「最大宽度」约束因 `fixedSize()` 位置错误而失效，长标签会溢出并遮挡相邻内容；展开标签流的容器也不会对首个超宽标签换行，导致内容横向溢出界面，两处均已修复

### 优化

- 状态栏图标按屏幕缩放倍率**预渲染为 18pt 位图**，避免菜单栏图层重建（进入/退出全屏、切换空间）时在线缩放 3MB 原图造成瞬时空白
- 监听空间切换、屏幕参数变化与应用激活，在这些时机重新提交图标并强制重绘，全屏唤出菜单栏时图标不再偶发空白
- 行内标签最大宽度 96pt → 64pt；时间戳、复制次数、`+N` 徽标提升布局优先级，空间不足时优先压缩标签，关键信息不再被挤出可视区

## [1.3.23] - 2026-09-06

### 新增

- 搜索支持标签：正文或标签命中都会返回（例如某条记录打了「安全」标签，搜索「安全」即可找到，即使正文里没有这两个字）
- Hover 时间戳行**原地展开完整标签流**（流式换行，最多 9 个 + `+N`），预览文本自动缩为一行补偿高度，移开后自动收回

### 优化

- 标签展示重构：行内最多显示 2 个标签胶囊，超出折叠为 `+N` 徽标；长标签自动截断，不再挤爆时间戳行
- 改用**固定自签名证书**（CopyListDev）签名：本次更新后重新授权一次辅助功能，此后所有版本更新不再需要重复授权

### 变更

- 新增 `Scripts/create-signing-cert.sh` 一键创建/检查签名证书；构建脚本在证书缺失时自动回退 ad-hoc 并提示；Release tag 包含完整源码

## [1.3.22] - 2026-09-05

### 新增

- 内容去重：同一段文本重复复制不再新增冗余记录，仅刷新时间戳与复制次数
- 图片按内容指纹去重：同一张图片反复复制不再落盘新文件，减轻存储占用

### 修复

- 搜索对 `%`、`_`、`\` 等特殊字符按字面匹配，避免搜索误导
- 阻止包含引号/反斜杠的标签破坏数据与匹配
- 数据库损坏时自动隔离为 `.corrupt` 备份再重建，保留坏文件供人工恢复
- 筛选标签被移除时自动退出筛选，避免列表「隐形筛选」莫名变空

### 优化

- GIF 动图保留原始字节，从浏览器等复制 GIF 不再被强制转成静态 PNG
- 编辑成重复内容时合并收藏/次数/标签，不丢失任何用户数据
- 历史上限清理绝不触碰收藏记录，也不误删仍被引用的图片文件
- 升级时自动清理旧版 AppleScript 登录项，避免双重自启
- 自动粘贴默认值同步（一次性迁移，老用户升级自动保持原有行为）

## [1.3.21] - 2026-07-10

### 变更

- 点击记录后的状态复位收敛为 `onSelect` 回调，搜索词/收藏夹/标签筛选统一恢复默认视图

### 文档

- README 更新日志同步

## [1.3.18] - 2026-06-26

### 修复

- **搜索状态未重置**：搜索并点击记录后，再次打开 Popover 仍显示上次的搜索关键词和过滤结果；现选中记录后自动清空搜索框、重置收藏/标签筛选状态，回到初始默认视图
- 同步修复主窗口（`MainWindowView`）中右键复制后搜索框未清空的问题

## [1.3.16] - 2026-06-26

### 变更

- 搜索粘贴修复源码正式入库并同步 README（无新增用户可见变更）

## [1.3.13] - 2026-06-17

### 修复

- **彻底解决搜索后点击记录无法自动粘贴和关闭 Popover**：根因是 `allowPasteThisTime` + `shouldPaste` 机制存在设计缺陷，搜索输入后标志被置为 false 且无法恢复；现移除该冗余机制，`ItemRow` 直接读取 `enableAutopaste` 开关

### 优化

- 清理死代码，简化粘贴判断逻辑

## [1.3.10] - 2026-06-17

### 修复

- 尝试修复搜索后点击记录无法自动粘贴的问题（初次修复不完整）

## [1.3.9] - 2026-06-16

### 新增

- 设置「关于」区块展示作者信息（作者、邮箱、GitHub）
- 邮箱点击调用系统邮件客户端，GitHub 点击打开作者主页

## [1.3.7] - 2026-06-16

### 修复

- 更新检查功能分支名称错误（main → master）

## [1.3.5] - 2026-06-16

### 变更

- 项目目录工程化改造：`Sources/`（App/Core/Views）、`Resources/`、`Scripts/`、`Docs/`

### 优化

- `build.sh` 适配新目录结构

## [1.3.2] - 2026-06-16

### 新增

- GitHub 自动化 Release 发布
- 构建时自动检测代码变更并递增版本号
- 设置界面动态显示版本号和构建时间
- 设置内检查更新按钮

### 修复

- 更新检查 task 未启动的问题

### 优化

- 每 5 分钟自动清理图片缓存，降低内存占用
- Popover 关闭时自动落盘待写数据

## [1.2.1] - 2025-06-11

### 新增

- 收藏夹自动备份（每小时，保留 10 个）
- 一键导出收藏夹为 JSON
- 代码签名支持，辅助功能权限持久化
- `build.sh` 自动构建脚本

### 修复

- 自动粘贴功能恢复正常
- 窗口崩溃问题

### 优化

- 回退到稳定的 Popover 窗口设计

## [1.2.0] - 2025-06-11

### 新增

- 右键删除单条记录
- 收藏夹标签功能（添加/移除/筛选）
- 标签可视化显示

### 优化

- 删除操作无需二次确认

## [1.1.0] - 2025-06-11

### 新增

- 开机自动启动功能
- 自动粘贴开关
- 内容自动去重

### 优化

- 商业级设置界面

## [1.0.0] - 2025-06-11

### 新增

- 初始版本发布
